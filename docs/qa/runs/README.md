# QA Run Reports

- Use `node scripts/run-domain-qa.mjs --bundle critical-priority --write-doc-report` to generate a dated Markdown sign-off report here.
- JSON and Markdown runtime artifacts are always written under `test-results/domain-qa/<timestamp>/`.
- Treat files in this directory as operator-facing sign-off snapshots, not source-of-truth configuration.
